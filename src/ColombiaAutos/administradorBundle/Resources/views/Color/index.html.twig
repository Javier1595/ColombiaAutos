{% extends '::base.html.twig' %}

{% block body -%}
    <div class="container">
        <h1><b>Colores</b></h1>
        
        <table class="records_list">
            <thead>
                <tr>
                    <th>Codigo color</th>
                    <th>Nombre color</th>
                    <!--<th>Actions</th>-->
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('color_show', { 'id': entity.codColor }) }}">{{ entity.codColor }}</a></td>
                    <td>{{ entity.nomColor }}</td>
                    <!--
                    <td>
                    <ul>
                        <li>
                            <a href="{{ path('color_show', { 'id': entity.codColor }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('color_edit', { 'id': entity.codColor }) }}">edit</a>
                        </li>
                    </ul>
                    -->
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <!--
        <ul>
            <li>
                <a href="{{ path('color_new') }}">
                    Crear un nuevo propietario
                </a>
            </li>
        </ul>
    -->
    </div>
{% endblock %}
