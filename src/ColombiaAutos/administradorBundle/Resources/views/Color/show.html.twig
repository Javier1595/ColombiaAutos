{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Color</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Codcolor</th>
                <td>{{ entity.codColor }}</td>
            </tr>
            <tr>
                <th>Nomcolor</th>
                <td>{{ entity.nomColor }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('color') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('color_edit', { 'id': entity.codColor }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
