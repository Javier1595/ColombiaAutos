{% if is_granted('ROLE_ADMIN') %}
{% extends '::base.html.twig' %}

{% block body -%}
<div class="container">
    <h1>Editar Carroceria</h1>
		<ul class="record_actions">
		<li>
			<a href="{{ path('carroceria') }}">
		    Volver
			</a>
		</li>
	</ul>
    {{ form(edit_form) }}
</div>
{% endblock %}
{% endif %}